import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-customers',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Customers</h1><p>All registered customers and their activity.</p></div></div><section class="card panel"><input class="input search" [(ngModel)]="search" (input)="load()" placeholder="Search customers..."><div class="table"><table><tr><th>Customer</th><th>Email</th><th>Phone</th><th>Status</th><th>Action</th></tr>@for(c of rows();track c.customerId){<tr><td><b>{{c.fullName}}</b></td><td>{{c.email}}</td><td>{{c.phone||'-'}}</td><td>{{c.status}}</td><td>@if(c.status==='Active'){<button class="small danger" (click)="off(c.customerId)">Deactivate</button>}@else{<button class="small success" (click)="on(c.customerId)">Reactivate</button>}</td></tr>}</table></div></section></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.search{max-width:300px;margin-bottom:12px}`]})export class CustomersComponent{private api=inject(AdminApiService);readonly rows=signal<any[]>([]);search='';constructor(){this.load()}load(){this.api.customers(this.search).subscribe((r:any)=>this.rows.set(r.items??r.data??r??[]))}off(id:number){if(confirm('Deactivate customer?'))this.api.deactivate(id).subscribe(()=>this.load())}on(id:number){this.api.reactivate(id).subscribe(()=>this.load())}}
