import { Component,inject,signal } from '@angular/core';import { AdminApiService } from '../admin-api.service';
@Component({selector:'app-admin-dashboard',template:`<main class="pagex"><div class="head"><div><h1>Dashboard</h1><p>Live operational overview across all venues and events.</p></div><span class="active">● Admin session active</span></div><div class="stats">@for(s of cards();track s.l){<article class="card"><span>{{s.l}}</span><b>{{s.v}}</b><small>{{s.n}}</small></article>}</div><div class="charts"><section class="card panel"><h2>Revenue by month</h2><div class="bars">@for(x of monthly();track x.label){<div><i [style.height.%]="height(x.netRevenue??x.revenue??0)"></i><small>{{x.label}}</small></div>}</div></section><section class="card panel"><h2>Operational alerts</h2>@for(a of alerts();track $index){<p class="alert"><b>{{a.severity||'Info'}}</b> {{a.message||a.title||a.type}}</p>}@empty{<p>No urgent alerts.</p>}</section></div></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.active{background:#dcfce7;color:#166534;padding:8px 11px;border-radius:9px;font-size:10px}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:14px}.stats article{padding:16px;display:flex;flex-direction:column;gap:6px}.stats span,.stats small{font-size:10px;color:var(--muted)}.stats b{font-size:24px}.charts{display:grid;grid-template-columns:1.2fr 1fr;gap:14px}.bars{height:210px;display:flex;align-items:end;gap:16px}.bars>div{height:100%;flex:1;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;gap:5px}.bars i{width:70%;min-height:4px;background:linear-gradient(#2a9d8f,#0f766e);border-radius:7px 7px 2px 2px}.bars small{font-size:8px}.alert{padding:9px;border-bottom:1px solid var(--border);font-size:11px}.alert b{color:#b45309}@media(max-width:950px){.stats{grid-template-columns:1fr 1fr}.charts{grid-template-columns:1fr}}`]})
export class DashboardComponent{private api=inject(AdminApiService);readonly d=signal<any>({});readonly monthly=signal<any[]>([]);readonly alerts=signal<any[]>([]);constructor(){this.api.dashboard().subscribe(x=>this.d.set(x));this.api.monthly().subscribe(x=>this.monthly.set(x));this.api.alerts().subscribe({next:x=>this.alerts.set(x),error:()=>this.alerts.set([])})}cards(){const d=this.d();return [{l:'Total customers',v:d.totalCustomers??0,n:'Active accounts'},{l:'Total events',v:d.totalEvents??0,n:'All events'},{l:'Total bookings',v:d.totalBookings??0,n:'Across platform'},{l:'Total revenue',v:'Rs. '+(d.totalRevenue??0),n:'Completed payments'},{l:'Available seats',v:d.availableSeats??0,n:'Live inventory'},{l:'Booked seats',v:d.bookedSeats??0,n:'Reserved'},{l:'Parking usage',v:d.reservedParking??0,n:'Booked slots'},{l:'Cancelled',v:d.cancelledBookings??0,n:'Bookings'}]}height(v:number){const m=Math.max(...this.monthly().map(x=>Number(x.netRevenue??x.revenue??0)),1);return Math.max(5,Number(v)/m*100)}}
