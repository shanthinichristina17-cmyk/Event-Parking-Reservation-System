import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-reports-admin',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Reports</h1><p>Operational and financial reporting.</p></div><div><button class="btn" (click)="download('bookings.csv')">Bookings CSV</button> <button class="btn primary" (click)="download('payments.csv')">Payments CSV</button></div></div><section class="card panel filter"><input class="input" [(ngModel)]="from" type="date"><input class="input" [(ngModel)]="to" type="date"><button class="btn primary" (click)="load()">Apply</button></section>@if(s();as x){<div class="stats"><article class="card"><span>Bookings</span><b>{{x.totalBookings||0}}</b></article><article class="card"><span>Confirmed</span><b>{{x.confirmedBookings||0}}</b></article><article class="card"><span>Revenue</span><b>Rs. {{x.totalRevenue||0}}</b></article><article class="card"><span>Refunds</span><b>Rs. {{x.totalRefunds||x.refundAmount||0}}</b></article></div>}<section class="card panel table"><h2>Revenue by event</h2><table><tr><th>Event</th><th>Bookings</th><th>Revenue</th></tr>@for(r of rev();track $index){<tr><td>{{r.eventName||r.name}}</td><td>{{r.bookingCount??r.confirmedBookings??0}}</td><td>Rs. {{r.revenue??0}}</td></tr>}</table></section></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.filter{display:grid;grid-template-columns:1fr 1fr auto;gap:8px;margin-bottom:14px}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:14px}.stats article{padding:16px;display:flex;flex-direction:column;gap:6px}.stats span{color:var(--muted);font-size:10px}.stats b{font-size:21px}@media(max-width:700px){.stats{grid-template-columns:1fr 1fr}.filter{grid-template-columns:1fr}}`]})export class ReportsComponent{private api=inject(AdminApiService);readonly s=signal<any>({});readonly rev=signal<any[]>([]);from='';to='';constructor(){this.load()}load(){this.api.summary(this.from,this.to).subscribe(x=>this.s.set(x));this.api.revenue(this.from,this.to).subscribe(x=>this.rev.set(x))}download(n:string){this.api.export(n).subscribe(b=>{const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=n;a.click();URL.revokeObjectURL(u)})}}
