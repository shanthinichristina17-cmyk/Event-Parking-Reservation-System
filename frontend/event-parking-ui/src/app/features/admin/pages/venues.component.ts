import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-venues',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Venues</h1><p>Configure partner venues.</p></div></div><section class="card panel add"><input class="input" [(ngModel)]="name" placeholder="Venue name"><input class="input" [(ngModel)]="address" placeholder="Address"><input class="input" [(ngModel)]="capacity" type="number" placeholder="Capacity"><button class="btn primary" (click)="add()">+ Add venue</button></section><div class="cards">@for(v of rows();track v.venueId){<article class="card panel"><span class="ico">⌂</span><h3>{{v.name}}</h3><p>{{v.address}}</p><b>Capacity {{v.capacity}}</b><button class="small danger" (click)="del(v.venueId)">Delete</button></article>}</div></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.add{display:grid;grid-template-columns:1fr 2fr 1fr auto;gap:8px;margin-bottom:14px}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.cards p{color:var(--muted);font-size:11px}.ico{width:32px;height:32px;border-radius:9px;background:#e9f7f4;color:var(--purple);display:grid;place-items:center}@media(max-width:800px){.add,.cards{grid-template-columns:1fr 1fr}}@media(max-width:500px){.add,.cards{grid-template-columns:1fr}}`]})export class VenuesComponent{private api=inject(AdminApiService);readonly rows=signal<any[]>([]);name='';address='';capacity:any='';constructor(){this.load()}load(){this.api.venues().subscribe(x=>this.rows.set(x))}add(){this.api.addVenue({name:this.name,address:this.address,capacity:Number(this.capacity)}).subscribe(()=>{this.name='';this.address='';this.capacity='';this.load()})}del(id:number){if(confirm('Delete venue?'))this.api.delVenue(id).subscribe(()=>this.load())}}
