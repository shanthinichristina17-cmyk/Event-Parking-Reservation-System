import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-categories',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Categories</h1><p>Group events so customers can filter quickly.</p></div></div><section class="card panel add"><input class="input" [(ngModel)]="name" placeholder="New category"><button class="btn primary" (click)="add()">+ Add</button></section><div class="cards">@for(c of rows();track c.categoryId){<article class="card panel"><h3>{{c.name}}</h3><small>Event category</small><button class="small danger" (click)="del(c.categoryId)">Delete</button></article>}</div></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.add{display:grid;grid-template-columns:1fr auto;gap:8px;margin-bottom:14px}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.cards article{display:flex;flex-direction:column;gap:10px}.cards small{color:var(--muted)}@media(max-width:800px){.cards{grid-template-columns:1fr 1fr}}`]})export class CategoriesComponent{private api=inject(AdminApiService);readonly rows=signal<any[]>([]);name='';constructor(){this.load()}load(){this.api.categories().subscribe(x=>this.rows.set(x))}add(){if(this.name.trim())this.api.addCategory(this.name).subscribe(()=>{this.name='';this.load()})}del(id:number){if(confirm('Delete category?'))this.api.delCategory(id).subscribe(()=>this.load())}}
