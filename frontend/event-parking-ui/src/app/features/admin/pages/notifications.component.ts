import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-notifications-admin',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Notifications</h1><p>Broadcast updates to active customers.</p></div></div><section class="card panel form"><select class="input" [(ngModel)]="type"><option>Update</option><option>Reminder</option><option>Announcement</option></select><textarea class="input" [(ngModel)]="message" placeholder="Message"></textarea><button class="btn primary" (click)="send()">Send broadcast</button>@if(result()){<p>{{result()}}</p>}</section><section class="card panel"><h2>Automatic notifications</h2><p>Payment, confirmation, cancellation and event reminders are already generated by the backend.</p></section></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.form{max-width:700px;display:flex;flex-direction:column;gap:10px;margin-bottom:14px}textarea{min-height:130px;padding:10px}`]})export class NotificationsComponent{private api=inject(AdminApiService);type='Update';message='';readonly result=signal('');send(){this.api.broadcast(this.type,this.message).subscribe({next:r=>{this.result.set(r.message??'Sent');this.message=''},error:e=>this.result.set(e?.error?.message??'Failed')})}}
