import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-bookings-admin',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Bookings</h1><p>Monitor all reservations and statuses.</p></div></div><section class="card panel"><div class="filters"><input class="input" [(ngModel)]="search" (input)="load()" placeholder="Search booking/customer"><select class="input" [(ngModel)]="status" (change)="load()"><option value="">All</option><option>Pending</option><option>Confirmed</option><option>Cancelled</option><option>Expired</option></select></div><div class="table"><table><tr><th>Booking</th><th>Customer</th><th>Event</th><th>Status</th><th>Total</th></tr>@for(b of rows();track b.bookingId){<tr><td><b>{{b.bookingNumber}}</b></td><td>{{b.customerName||b.customerEmail||b.customerId}}</td><td>{{b.eventName}}</td><td>{{b.status}}</td><td>Rs. {{b.finalTotal??b.totalAmount}}</td></tr>}</table></div></section></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.filters{display:grid;grid-template-columns:2fr 1fr;gap:8px;margin-bottom:12px}`]})export class BookingsComponent{private api=inject(AdminApiService);readonly rows=signal<any[]>([]);search='';status='';constructor(){this.load()}load(){this.api.bookings(this.status,this.search).subscribe(x=>this.rows.set(x))}}
