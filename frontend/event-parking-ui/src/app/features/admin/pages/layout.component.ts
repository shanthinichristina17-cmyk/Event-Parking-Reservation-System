import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AdminApiService } from '../admin-api.service';@Component({selector:'app-layout-builder',imports:[FormsModule],template:`<main class="pagex"><div class="head"><div><h1>Seats & Parking</h1><p>Generate event layouts without exceeding event capacity.</p></div></div><section class="card panel controls"><select class="input" [(ngModel)]="eventId" (change)="load()"><option [ngValue]="0">Select event</option>@for(e of events();track e.eventId){<option [ngValue]="e.eventId">{{e.name}}</option>}</select><input class="input" [(ngModel)]="rows" type="number" placeholder="Rows"><input class="input" [(ngModel)]="cols" type="number" placeholder="Columns"><button class="btn primary" (click)="makeSeats()">Generate seats</button><input class="input" [(ngModel)]="zone" placeholder="Zone"><input class="input" [(ngModel)]="slots" type="number" placeholder="Slots"><input class="input" [(ngModel)]="fee" type="number" placeholder="Fee"><button class="btn primary" (click)="makeParking()">Generate parking</button></section><h2>Seat map</h2><div class="seatmap">@for(s of seatRows();track s.seatId){<span [class.busy]="s.status!=='Available'">{{s.seatRow}}{{s.seatNumber}}</span>}</div><h2>Parking map</h2><div class="parkmap">@for(p of parkRows();track p.slotId){<span [class.busy]="p.status!=='Available'">{{p.slotNumber}}<small>{{p.parkingType}}</small></span>}</div></main>`,styles:[`.pagex{padding:28px}.head{display:flex;justify-content:space-between;gap:15px;align-items:center;margin-bottom:18px}.head h1{font-size:30px;margin:0}.head p{color:var(--muted);margin:4px 0}.panel{padding:18px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:680px}th,td{text-align:left;padding:11px;border-bottom:1px solid var(--border);font-size:11px}th{color:var(--muted)}.small{border:0;border-radius:8px;padding:7px 9px;cursor:pointer}.danger{background:#fff1f2;color:#be123c}.success{background:#ecfdf3;color:#067647}@media(max-width:600px){.pagex{padding:15px}.head{align-items:flex-start;flex-direction:column}}.controls{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.seatmap,.parkmap{display:grid;grid-template-columns:repeat(auto-fit,minmax(60px,1fr));gap:8px}.seatmap span,.parkmap span{background:#fff;border:1px solid var(--border);border-radius:9px;padding:10px;text-align:center;font-size:10px;font-weight:800}.parkmap span{min-height:65px;display:flex;flex-direction:column;justify-content:center}.parkmap small{font-size:8px;color:var(--muted)}.busy{background:#fff1f2!important;color:#991b1b}@media(max-width:700px){.controls{grid-template-columns:1fr 1fr}}`]})export class LayoutComponent{private api=inject(AdminApiService);readonly events=signal<any[]>([]);readonly seatRows=signal<any[]>([]);readonly parkRows=signal<any[]>([]);eventId=0;rows=4;cols=5;zone='A';slots=5;fee=500;constructor(){this.api.events().subscribe(x=>this.events.set(x))}load(){if(!this.eventId)return;this.api.seats(this.eventId).subscribe(x=>this.seatRows.set(x));this.api.parking(this.eventId).subscribe(x=>this.parkRows.set(x))}makeSeats(){this.api.genSeats(this.eventId,{rows:this.rows,columns:this.cols,regularPrice:1500,premiumPrice:1900,vipPrice:2300}).subscribe({next:x=>this.seatRows.set(x),error:e=>alert(e?.error?.message??'Seat generation failed')})}makeParking(){this.api.genParking(this.eventId,{zones:[{zone:this.zone,parkingType:'Standard',slotCount:this.slots,fee:this.fee}]}).subscribe({next:x=>this.parkRows.set(x),error:e=>alert(e?.error?.message??'Parking generation failed')})}}
