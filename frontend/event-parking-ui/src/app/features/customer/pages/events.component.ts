import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { RouterLink } from '@angular/router';import { CustomerApiService } from '../customer-api.service';
@Component({selector:'app-events',imports:[FormsModule,RouterLink],template:`<main class="page wrap"><h1>Browse events</h1><p>Search and reserve live event inventory.</p><div class="card filters"><input class="input" [(ngModel)]="search" placeholder="Search events"><button class="btn primary" (click)="load()">Search</button></div><div class="grid">@for(e of items();track e.eventId){<article class="card item"><div class="art">LIVE</div><h3>{{e.name}}</h3><p>{{e.eventDate}} · {{e.startTime}}</p><p>{{e.venueName||'Venue'}}</p><strong>Rs. {{e.ticketPrice}}</strong><a class="btn primary" [routerLink]="['/booking',e.eventId]">Select seats</a></article>}</div></main>`,styles:[`.wrap{padding:42px 0}.wrap h1{font-size:40px;margin:0}.wrap>p,.item p{color:var(--muted)}.filters{padding:14px;display:grid;grid-template-columns:1fr auto;gap:8px;margin:20px 0}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.item{padding:16px}.art{height:130px;margin:-16px -16px 14px;background:var(--gradient);display:grid;place-items:center;color:#fff;font-weight:900}.item strong{display:block;color:var(--purple);font-size:18px;margin:14px 0}@media(max-width:780px){.grid{grid-template-columns:1fr 1fr}}@media(max-width:500px){.grid{grid-template-columns:1fr}}`]})
export class EventsComponent{private api=inject(CustomerApiService);readonly items=signal<any[]>([]);search='';constructor(){this.load()}load(){this.api.events({search:this.search}).subscribe((r:any)=>this.items.set(r.items??r.data??r??[]))}}
