import { Component,inject,signal } from '@angular/core';import { CustomerApiService } from '../customer-api.service';
@Component({selector:'app-my-bookings',template:`<main class="page wrap"><h1>My bookings</h1><p>Tickets, parking and refunds in one place.</p><div class="tabs"><button (click)="load('upcoming')">Upcoming</button><button (click)="load('past')">Past</button><button (click)="load('cancelled')">Cancelled</button></div>@for(b of items();track b.bookingId){<article class="card booking"><div class="thumb">EP</div><div><h3>{{b.eventName}}</h3><p>{{b.eventDate}} · {{b.startTime}} · {{b.venueName}}</p><small>{{b.bookingNumber}}</small></div><div class="right"><span>{{b.status}}</span><b>Rs. {{b.finalTotal}}</b>@if(b.status==='Confirmed'){<button (click)="cancel(b.bookingId)">Cancel</button>}</div></article>}</main>`,styles:[`.wrap{padding:40px 0}.wrap h1{font-size:38px;margin:0}.wrap>p,.booking p,.booking small{color:var(--muted)}.tabs{display:flex;gap:7px;margin:20px 0}.tabs button{border:1px solid var(--border);background:#fff;border-radius:99px;padding:8px 11px}.booking{display:grid;grid-template-columns:75px 1fr auto;align-items:center;gap:14px;padding:13px;margin-bottom:12px}.thumb{height:65px;border-radius:10px;background:var(--gradient);color:#fff;display:grid;place-items:center}.booking h3{margin:0}.right{text-align:right;display:flex;flex-direction:column;gap:5px}.right span{color:var(--success);font-size:11px}.right b{color:var(--purple)}.right button{border:0;background:transparent;color:var(--danger)}@media(max-width:550px){.booking{grid-template-columns:60px 1fr}.right{grid-column:1/-1;text-align:left}}`]})
export class MyBookingsComponent{private api=inject(CustomerApiService);readonly items=signal<any[]>([]);constructor(){this.load('upcoming')}load(t:string){this.api.bookings(t).subscribe(x=>this.items.set(x))}cancel(id:number){if(confirm('Cancel booking?'))this.api.cancel(id).subscribe(()=>this.load('upcoming'))}}
