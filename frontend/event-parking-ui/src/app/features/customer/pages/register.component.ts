import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router,RouterLink } from '@angular/router';import { AuthService } from '../../../core/services/auth.service';
@Component({selector:'app-register',imports:[FormsModule,RouterLink],template:`<section class="auth"><div class="art"><b>EP EventPark</b><div><h2>Seats and parking,<br>booked together.</h2><p>Create one account and keep tickets, parking and reminders together.</p></div></div><form class="card" (ngSubmit)="go()"><h1>Create your account</h1><label>Full name<input class="input" [(ngModel)]="name" name="n"></label><label>Email<input class="input" [(ngModel)]="email" name="e"></label><label>Phone<input class="input" [(ngModel)]="phone" name="p"></label><label>Password<input class="input" [(ngModel)]="password" name="pw" type="password"></label>@if(error()){<small class="err">{{error()}}</small>}<button class="btn primary">Create account</button><p>Already registered? <a routerLink="/login">Sign in</a></p></form></section>`,styles:[`.auth{min-height:100vh;display:grid;grid-template-columns:1fr 1fr}.art{padding:50px;background:var(--gradient);color:#fff;display:flex;flex-direction:column;justify-content:space-between}.art h2{font-size:44px;line-height:1}.card{width:min(440px,calc(100% - 30px));margin:auto;padding:30px;display:flex;flex-direction:column;gap:13px}.card h1{font-size:32px}.card label{display:flex;flex-direction:column;gap:6px;font-size:12px;font-weight:700}.err{color:var(--danger)}@media(max-width:700px){.auth{grid-template-columns:1fr}.art{display:none}}`]})
export class RegisterComponent{private a=inject(AuthService);private r=inject(Router);name='';email='';phone='';password='';readonly error=signal('');go(){this.a.register({fullName:this.name,email:this.email,phone:this.phone,password:this.password}).subscribe({next:()=>this.r.navigateByUrl('/login'),error:e=>this.error.set(e?.error?.message??'Registration failed')})}}
