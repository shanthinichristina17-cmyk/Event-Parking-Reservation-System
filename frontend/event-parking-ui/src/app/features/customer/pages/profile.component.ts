import { Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { AuthService } from '../../../core/services/auth.service';import { CustomerApiService } from '../customer-api.service';
@Component({selector:'app-profile',imports:[FormsModule],template:`<main class="page wrap"><h1>Profile</h1><section class="card form"><label>Full name<input class="input" [(ngModel)]="m.fullName"></label><label>Email<input class="input" [value]="m.email||''" disabled></label><label>Phone<input class="input" [(ngModel)]="m.phone"></label><button class="btn primary" (click)="save()">Save profile</button>@if(msg()){<p>{{msg()}}</p>}</section></main>`,styles:[`.wrap{padding:40px 0}.form{max-width:540px;padding:22px;display:flex;flex-direction:column;gap:13px}.form label{display:flex;flex-direction:column;gap:6px;font-size:12px;font-weight:700}`]})
export class ProfileComponent{private api=inject(CustomerApiService);private a=inject(AuthService);m:any={};readonly msg=signal('');constructor(){this.api.me().subscribe(x=>this.m=x)}save(){const id=this.a.user()?.customerId;if(id)this.api.updateMe(id,{fullName:this.m.fullName,phone:this.m.phone}).subscribe({next:()=>this.msg.set('Profile updated'),error:e=>this.msg.set(e?.error?.message??'Update failed')})}}
